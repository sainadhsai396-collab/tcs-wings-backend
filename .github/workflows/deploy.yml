name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Railway Deploy
        uses: withastro/action@v0
        with:
          package-manager: npm@latest